print(
	'################################################################# START #################################################################'
);

db = db.getSiblingDB('covid-slayer');
db.createUser({
	user: 'root',
	pwd: 'root',
	roles: [{ role: 'readWrite', db: 'covid-slayer' }]
});
db.createCollection('users');

print(
	'################################################################# END #################################################################'
);
